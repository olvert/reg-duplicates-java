package structure;

/**
 * Created by ollesvensson on 08/10/16.
 */
public interface RegStructure {

    /*
     * Should insert reg number into data structure and return true if number does not already exist. False otherwise.
     */
    boolean insert(String reg);
}
